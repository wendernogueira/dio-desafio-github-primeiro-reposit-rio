GIt comando
git add "meucodigo.txt" (adiciona somente um arquivo)
git add . (adiciona vários arquivos

git init (iniciar dentro da pasta do git)
git status (ver status)

git push (manda os arquivos para site do git) 
Definir o local para onde vai git remote add origin(que é alias(apelido)) link do projeto

git commit -m "texto explicando o que foi adicionado na linha"
    depois fazer o
git push (manda as atualizações)

git reflog (ver histórico)

git reset --hard (código da versão que quer volta(para ver só código git reflog))

git branch ( ver as branch que está indo os push)

criar um branch git branch (nome que queria da(exemplo: git branch staging)) 
mudar de branch master ou para outro git checkout staging

unir a versão de código para master /Digita git checkout master que vai para principal que normalmente é a master
Aviso lembrar de baixar a versão que está online usando o comando GIT PULL
git merge staging (vai puxar os dados staging para a master)
git pull( vai trazer atualização dis códigos que está no github

criar um branch a partir de outra branch git checkout -b sistema-de-login master
git commit -m "sistema de login"
